<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.babenkovladimir">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".resource.ResourceActivity"></activity>
    <activity android:name=".preferences.PreferencesActivity" />
    <activity android:name=".dialog.DialogActivity" />
    <activity android:name=".master_detail_flow.AddCardActivity" />
    <activity android:name=".master_detail_flow.BankCardAboutActivity" />
    <activity android:name=".fragments.FragmentsActivity" />
    <activity android:name=".master_detail_flow.MasterDetailFlowActivity" />
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>